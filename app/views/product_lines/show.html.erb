<table>
  <thead>
    <th>Version</th>
    <th>Products</th>
    <th>Published at</th>
    <td colspan="2"></td>
  </thead>
  <tbody>
    <%= render partial: 'show_row', locals: { index: "Latest", object: @product_line } %>

    <% index = @product_line.parents.count %>
    <% @product_line.parents.each do |object| %>
      <%= render partial: 'show_row', locals: { index: index, object: object } %>
      <% index = index - 1 %>
    <% end %>
  </tbody>
</table>
